# Formeln – Seite 201 (fototreu)

**Quelle:** [`s201.md`](s201.md) — Status weiterhin `OCR-Rohfassung – noch nicht
menschlich verifiziert`. Diese Extraktion entsteht auf ausdrücklichen Wunsch
von Werner vor Abschluss der Prüfstellen in `s201.md` (Walking Skeleton). Jede
hier erfasste Stelle bleibt bis zur Bestätigung am Original offen.

`s201.md` hatte ursprünglich vermerkt, die Seite enthalte „keine eigene
Formel, da die Seite nur konstruktive Anweisungen ohne Rechenwert enthält"
(Prüfstelle 7, noch unbestätigt). Beim Blick in die Zeichnung
`skizzen/s201_skizze_02.png` finden sich jedoch mehrere zahlenwertgebundene
Konstruktionsangaben, die unten als zeichnungsgebundene Stellen erfasst sind.
Prüfstelle 7 in `s201.md` ist damit ebenfalls berührt und sollte mit Werner
neu abgeglichen werden.

## Aus dem Fließtext (`ocr_s201.md`)

### Übertrag vAchsel/hAchsel auf die Kugelkurven

Quelle: `ocr_s201.md`, Zeile 26–27 (Abschnitt „5 Ärmelpunkte").

```text
An der unteren Armelkurve die vAchsel auf die vAk übertragen → vorderer
Armelpunkt = vAP.
Die hAchsel auf die hAk übertragen → hinterer Armelpunkt = hAP.
```

Hinweis: OCR gibt „Ä" durchgehend als „A" wieder (bereits in `s201.md`,
Abschnitt „Bekannte auffällige Stellen", dokumentiert). Wortlaut hier
unverändert aus `ocr_s201.md` übernommen.

### Knips-Abstand oberhalb hAP

Quelle: `ocr_s201.md`, Zeile 28.

```text
Ein zweiter Knips wird 1 cm oberhalb des hAP markiert.
```

### Bezeichnung SuP = P5

Quelle: `ocr_s201.md`, Zeile 29.

```text
Der SuP ist hier der Konstruktionspunkt P5.
```

Hinweis: Identitätsangabe zwischen zwei Punktbezeichnungen, keine
Rechenformel im engeren Sinn; aus Vorsicht dennoch als coderelevante
Beziehung mit erfasst.

### Halbierung Saumlinie und ÄSaW-Antrag

Quelle: `ocr_s201.md`, Zeile 33.

```text
Die Saumlinie halbieren und von dort jeweils die 1/2 Armelsaumweite (ASaW)
nach links und rechts abtragen.
```

### Kontrolle OaW mit Abweichung

Quelle: `ocr_s201.md`, Zeile 36.

```text
Die OaW kontrollieren (hier 0,2 cm geringer als geplant).
```

## Aus der Zeichnung `skizzen/s201_skizze_01.png` (Label „□5 Ärmelkugel", zeichnungsgebunden)

### Lage Punkt 15

```text
15: besser knapp oberhalb der Hälfte (½)
```

Hinweis: Beschriftung an der Scheitelregion von vÄk/hÄk, mit Bezugslinie
„½" zwischen den beiden Winkellinien. Zeichnungsgebundene Auswahlregel für
die Lage von Punkt 15, nicht im Fließtext von `ocr_s201.md` enthalten.

## Aus der Zeichnung `skizzen/s201_skizze_02.png` (Label „□6 Ärmelpunkte, Ärmelnähte und Saum", zeichnungsgebunden)

### hAchsel-Wert

```text
hAchsel 7,3 cm → hÄP (üb)
```

Hinweis: Beschriftung „hAchsel 7,3 cm" an der hÄk-Kurve zwischen tP und
hÄP, Wert am unteren hÄP als „üb ... 7,3 cm" wiederholt.

### vAchsel-Wert

```text
vAchsel 4,1 cm → vÄP (üb)
```

Hinweis: Beschriftung „vAchsel 4,1 cm" an der vÄk-Kurve zwischen tP und
vÄP, Wert am unteren vÄP als „üb 4,1 cm" wiederholt.

### Knips 1 cm (Punkt 16)

```text
1 cm (Punkt 16, oberhalb hAP)
```

Hinweis: Pfeil und Zahl „1 cm" unmittelbar bei Punkt 16, deckt sich mit dem
Fließtext „1 cm oberhalb des hAP".

### Kontrolle der Oberarmweite (Punkt 20)

```text
Kontrolle der Oberarmweite (28,8 cm)
```

Hinweis: Horizontale Kontrolllinie auf Höhe der SuP-Lotlinie, links und
rechts davon jeweils mit „½" beschriftet; Punkt 20 markiert die rechte
Seite dieser Linie. Zahlenwert 28,8 cm nur in der Zeichnung, nicht im
Fließtext.

### Halbe Ärmelsaumweite (Punkt 17)

```text
jeweils ½ Ärmelsaumweite (ÄSaW)
```

Hinweis: Beschriftung am Saum bei Punkt 17, deckt sich mit dem Fließtext
„jeweils die 1/2 Armelsaumweite (ASaW) nach links und rechts abtragen".

## Nicht erfasst

- Punkte 18, 19, 21 sind Pfeil-/Lagemarkierungen (Fadenlaufrichtung,
  Nahtverlauf) ohne eigenen Zahlenwert oder Rechenbeziehung.
- Bildunterschriften „6 Ärmelpunkte, Ärmelnähte und Saum" und „7 Fertiger
  enger Ärmel" sowie die Kopfzeile sind reine Beschriftungen, keine Formeln.
