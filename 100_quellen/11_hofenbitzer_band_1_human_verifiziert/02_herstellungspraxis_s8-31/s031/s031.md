# Seite 31 – Schnittteilliste und Lagerung

**Status: OCR-Rohfassung – noch nicht menschlich verifiziert**

Text, Werte, Tabellen und Zeichnungslabels dieser Seite sind erst nach
Werners ausdrücklicher Prüfung am Original als menschlich verifiziert zu
behandeln. Die OCR-Rohfassung ist keine Fachwahrheit.

## Belege

- Originalbild: [`../../../20_hofenbitzer_band_1_bilder/01_Photos_hofenb_ba1_total/s31.jpg`](../../../20_hofenbitzer_band_1_bilder/01_Photos_hofenb_ba1_total/s31.jpg)
- OCR-Markdown: [`ocr_s031.md`](ocr_s031.md)
- Layout-JSON (Mistral, dieselbe Antwort wie das Markdown): [`ocr_s031_raw.json`](ocr_s031_raw.json)
- Erkannte Tabellen: [`tabellen_s031.md`](tabellen_s031.md)
- Erkannter Skizzenbereich: [`skizzen_s031.json`](skizzen_s031.json)
- Kontaktbogen des Skizzenausschnitts: [`skizzen_kontaktbogen.jpg`](skizzen_kontaktbogen.jpg)

## Anmerkungen zur OCR-Eingabe

- Das Originalfoto zeigt die Seite vollständig, ohne erkennbaren
  Nachbarseitenanschnitt. Für die OCR wurde das unrotierte Originalbild
  verwendet; der erkannte Skizzenausschnitt wurde danach entsprechend der
  EXIF-Orientierung aufrecht gedreht (Beleg in `skizzen_s031.json`).
- Die Layout-Antwort meldet für diese Seite zwei Tabellen (`tbl-0`, `tbl-1`,
  siehe `tabellen_s031.md`) und eine Bildregion (`img-0.jpeg`, siehe
  `skizzen_s031.json`).

## Bekannte auffällige Stellen im Rohtext (noch nicht bestätigt)

- Fließtext, Abschnitt „Lagerung von Schnitten": Die OCR gibt „aufgefaßelt"
  wieder; im Foto wirkt das Wort eher wie „aufgefädelt". Am Original zu
  klären.
- Tabelle `tbl-0` (Punkt- und Linien-Namen): mehrere Sonderzeichen als „�"
  sowie die Kürzel „VM"/„UT"/„StKa" auffällig – Details in
  `tabellen_s031.md`.
- Tabelle `tbl-1` (Schnittteilliste): Die Anzahl-Spalte wirkt ab Zeile 9 um
  eine Zeile verschoben, außerdem Auffälligkeiten bei den Materialangaben in
  Zeile 25, 28, 30 und 31 – Details in `tabellen_s031.md`.

## Prüfstellen für Werner

1. Wortlaut und Absatzfolge der Fließtextabschnitte „Verwendung der
   Schnittteilliste" und „Lagerung von Schnitten" vollständig gegen das
   Original lesen, insbesondere „aufgefaßelt" vs. „aufgefädelt".
2. Tabelle `tbl-0` „Punkt- und Linien-Namen" Zeile für Zeile gegen das
   Original prüfen: Kürzel „VM" (statt „vM"?), die beiden „UT"-Zeilen (erste
   vermutlich „ÜT" für Übertritt), „StKa" (vermutlich „SlKa"), sowie alle als
   „�" markierten Sonderzeichen (Ä/Ü/ß).
3. Tabelle `tbl-1` „Schnittteilliste" Zeile für Zeile gegen das Original
   prüfen, insbesondere:
   - ob die Anzahl-Spalte ab Zeile 9 („Oberkragen") tatsächlich um eine Zeile
     verschoben aus der OCR kommt (siehe Detailbeleg in `tabellen_s031.md`);
   - die Materialangaben in Zeile 25 („RT-Armloch"), 28 („RT-Schlitz"), 30
     („Plack 2") und 31 („Bügelschablone Kantenabstich");
   - ob die Kopfspalten „No. intern" und „Anzahl" im Original tatsächlich
     getrennt sind.
4. Die Skizze `skizzen/s031_skizze_01.png` (Jackett-Zeichnung „Toscana 035")
   gegen das Original bestätigen; die OCR hat sie nur als Bildplatzhalter
   ausgegeben, ohne Beschriftung.
5. Bestätigen, dass die Seitenzahl „31" korrekt und die Seite vollständig
   erfasst ist (kein abgeschnittener Rand).
